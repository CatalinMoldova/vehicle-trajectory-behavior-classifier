sequence_length: 60
num_classes: 3
test_size: 0.2
random_state: 42
batch_size: 32
epochs: 20
learning_rate: 0.001
model: cnn_lstm

data:
  sample_path: data/sample/sample_trajectories.csv

artifacts_dir: artifacts
results_dir: results
